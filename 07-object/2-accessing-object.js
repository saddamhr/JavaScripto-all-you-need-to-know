var point = {
    x : 10,
    y : 20,
    z : 15
}
// Dot Notation
console.log(point.x)
console.log(point.y)
console.log(point.z)
console.log(point.x + point.y + point.z)

// Array Notation
console.log(point['x'])
console.log(point['z'])

var show = 'x'
console.log(point[show])